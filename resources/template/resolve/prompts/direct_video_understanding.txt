请分析以下视频内容，输出结构化的理解结果。
视频URL: {video_url}
视频时长: {video_duration}秒

请严格按照以下JSON格式输出：
```json
{output_schema}
```

要求：
1. 分析整个视频的完整内容，包括画面、台词、背景音乐、音效、字幕等所有元素
2. video字段必须严格遵循video.json模板结构，包含所有要求的字段：
   - 基本信息：商品信息、商品名称、商品分类、目标人群、剧本、模板、摘要等
   - 策略框架：核心模板、钩子策略、叙事流程、情绪流动曲线、节奏策略、张力策略、转化安排、片段间关系等
3. slices字段是数组，每个元素严格遵循slice.json模板结构，包含所有要求的字段：
   - 片段模板：模板名称、模板类型、机制、总结等
   - 创作要素：音频域、画面视觉域、情绪心理域、文案话术域、商品营销域等所有子字段
4. 将视频拆分为多个逻辑片段，每个片段时长3-10秒，时间范围准确
5. 所有字段内容必须基于视频实际内容，不得编造
6. 确保输出是严格的JSON格式，不要有任何额外的解释文本、markdown标记或其他内容